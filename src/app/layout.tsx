import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Lexington Painting, Inc. - Lexington Painting",
  description:
    "Sharing my love and passion as a painting contractor for satisfying customers with every brush stroke at a time. Lexington Painting and our team strive for 100% customers satisfaction guaranteed at all times.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}
